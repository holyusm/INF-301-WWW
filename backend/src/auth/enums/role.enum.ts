// Canonical roles for Fukusuke — kept in sync with UserRole in user.entity.ts
export enum Role {
  ADMIN = 'admin',
  DUENO = 'dueno',
  CAJERO = 'cajero',
  DESPACHADOR = 'despachador',
  CLIENTE = 'cliente',
}
