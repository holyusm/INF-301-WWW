// Roles canónicos de Fukusuke — equivalente a `UserRole` en credential.entity.ts
export enum Role {
  ADMIN = 'admin',
  DUENO = 'dueno',
  CAJERO = 'cajero',
  DESPACHADOR = 'despachador',
  CLIENTE = 'cliente',
}
